public enum GameType
{
    VSCOM, VSLocal, VSOnline
}
public enum PowerUp
{
    slow, speed, stun, shrink, grow, heal, damage, split, fastBall, magnet, doubleDamage
}

public enum InputType
{
    Touchscreen, Gyro, Both
}